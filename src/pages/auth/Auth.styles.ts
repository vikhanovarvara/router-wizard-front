import { createSx } from 'themes';

export const sx = createSx({
  wrapper: {
    justifyContent: 'center',
  },
  info: {
    display: 'flex',
    alignItems: 'center',
  },
  form: {
    display: 'flex',
    alignItems: 'center',
  },
});

export default sx;
